---
title: JPYC対応 SBTスタンプ型ウォレット & QRコード決済アプリ開発記録
emoji: 🪪
type: "tech" # tech / idea
topics: ["web3","jpyc","sbt","nextjs","polygon","avalanche","wallet","qr"]
published: false
---

# JPYC専用 SBTスタンプ対応ウォレット & QRコード決済アプリ 開発記録

本記事は、JPYC（日本円ステーブルコイン）での QR コード決済体験と、SBT（Soulbound Token）をスタンプカードのように“集める楽しさ”へ昇華させる Web アプリ開発の過程で得た知見をまとめたものです。MetaMask 等のウォレット操作に不慣れな方でも利用しやすく、コレクション性・来店回数の可視化による体験価値向上を目指しました。

## 開発した思い

JPYC専用にして MetaMask などのアプリに不慣れな人も、ステーブルコイン JPYC の QR コード決済を体験してもらいたい。

JPYC が店舗に普及することで、キャッシュレス決済の普及促進に寄与したい。

SBT をスタンプカードのように利用することで、個性あるお店の SBT が増えてコレクション性が出る。そして「コレクションしたい」という思いから、JPYC 決済して SBT が貰えるお店を利用したいと思うことを期待している。

## 特徴

1. 決済履歴に位置情報を付与し、どこの店舗かを直感的に把握できる。
2. EIP-681 規格対応で、店頭印刷された MetaMask 等ウォレットで読み取れる標準化 QR コードへも対応。
3. QR コード支払い・手動送付の双方でメモ（備考）を保存可能。
4. テストネットワークと本番ネットワークの両方に対応し学習／検証が容易。
5. SBT ギャラリー機能により、店舗発行 SBT をコレクション可能。
6. 来店回数（取得 SBT や履歴）から顧客ステージ表示が可能。

## 技術スタック概要

| 分類 | 採用技術 | 目的 |
|------|----------|------|
| フロント | Next.js (App Router) + TypeScript | 迅速開発 & 型安全 |
| UI | Tailwind CSS | 軽量スタイリング |
| PWA | `manifest.json` + Service Worker | オフライン体験・インストール促進 |
| ウォレット接続 | MetaMask / EIP-1193 | 署名・送金・チェーン情報取得 |
| トークン | JPYC / tJPYC (ERC-20) | 本番 & テスト決済検証 |
| SBT | 独自コントラクト (Soulbound) | 来店スタンプ表現 |
| 計測 | Google Analytics (gtag) | 利用動線解析 |
| 位置情報 | HTML Geolocation API | 決済履歴への紐付け |

## アーキテクチャ / 構成

```text
src/
  app/               # Next.js App Router ページ
  components/        # UI & 機能コンポーネント
  contracts/         # コントラクト呼び出しラッパ
  utils/             # 汎用ユーティリティ (sbt, network, paymentHistory 等)
  types/             # 型定義
public/              # PWA, QR関連静的ファイル
```

主な責務分離:

- `PaymentProcessor.tsx` / `ManualPaymentProcessor.tsx`: 送金ロジックとUI分離
- `SBTGallery.tsx`: SBT コレクション表示
- `SBTSyncChecker.tsx`: ウォレットとローカルの同期確認
- `GoogleAnalytics.tsx`: ルーティング連動の PV 計測

## EIP-681 と QR コード

EIP-681 は Ethereum 対応ウォレットが解釈可能な支払いリクエスト URI 形式: `ethereum:<target>@<chain_id>/transfer?address=<to>&uint256=<value>` 等。
本プロジェクトでは店舗掲示用 QR コードを EIP-681 準拠にすることで:

- ウォレット標準スキャン → 直接送金フォーム生成
- 金額・宛先のヒューマンエラー削減

加えて独自拡張（メモや店舗 ID）の内部保持用 QR 仕様も整理し、将来的な互換層を確保。

## SBT を“スタンプ”にする設計

SBT（譲渡不可トークン）を来店スタンプ化することで:

- “転売価値”ではなく“体験記録価値”を重視
- 店舗ごとのビジュアル差異でコレクション欲を喚起
- 来店回数によるステージ / ランク UI（例: Bronze → Silver → Gold）

内部的には取得履歴 + SBT メタデータを組み合わせ、フロントのみで段階表示（将来は集計をバックエンド化予定）。

## Remix を使いテストトークンを作成

Polygon Amoy / Avalanche Fuji テストネットに JPYC (テスト版) が見当たらなかったため、Remix を用いて ERC-20 対応の tJPYC をミント。これは私のみが操作可能な検証用トークンです。コントラクトアドレス等を差し替えることで各自のテストトークンにも容易に適用可能。

ポイント:

- 最低限の ERC-20 実装 + ミント権限制御
- 少額トークンで UI / 送金フロー / SBT 付与連動を反復検証

## データ保存と将来構想

現状: ブラウザ LocalStorage を利用（即時・手軽 / オフライン互換）。
課題: 端末間同期不可 / 改ざんリスク / 分析困難。
将来: 認証付きバックエンド + 永続 DB（例: PostgreSQL + Prisma / あるいは Indexer + The Graph）に移行し、

- 集計高速化（来店ステージ判定）
- 不正検知 / 監査ログ
- マーケティング分析（滞在時間 / 再訪率）

## 開発過程で学んだこと / 知見

### 1. UX と Web3 の“摩擦”低減

- 初回ウォレット接続時の心理障壁 → UI で送金フローを段階化し説明ツールチップ追加。
- QR コード決済は“押すだけ”の感覚に近づける（URI 自動解析）。

### 2. 標準規格を軸にカスタム拡張

- EIP-681 をベースにすることで互換性確保 + 学習コスト削減。
- 独自メモや位置情報などはアプリ側でメタレイヤー管理し、URI 仕様に過度な変更を加えない。

### 3. SBT 利用のベストプラクティス感触

- “所有”より“記録”重視の UI によって違和感減少。
- 来店進捗 = ゲーミフィケーション要素の最小構成が早期に価値を示す。

### 4. テストネット多重対応の利点

- ユーザー教育: ネットワーク切替 UI でチェーン概念を自然学習。
- デプロイやガス挙動差異を早期検証 → 本番トラブル軽減。

### 5. フロント主導の段階実装

- まずローカルストレージ + 軽量コントラクト呼出で仮説検証。
- 価値検証後にバックエンド化ロードマップを敷く“Lean”進行が有効。

## 課題と対応

| 課題 | アプローチ | 結果 |
|------|------------|------|
| ウォレット操作に不慣れ | ステップ化 UI / ガイドコンポーネント | 離脱率低下（仮説） |
| QR コード多様形式 | EIP-681 + 内部拡張デコーダ二層構造 | 実装複雑度抑制 |
| 同期不能 (LocalStorage) | 早期は割切り / 将来 DB 設計メモ化 | 初期工数最小化 |
| 位置情報精度 | HTML API + 許可失敗フォールバック | 失敗時は未記録扱い |
| SBT 表示遅延 | キャッシュ + 取得順序最適化 | 体感改善 |

## セキュリティ / 信頼性メモ（現段階）

- 送金トランザクションはユーザー署名必須（意図しない送金防止）。
- QR コード内容は必ず表示確認を挟む（サイレント送金を避ける）。
- LocalStorage 改ざんリスクは“参考表示”扱いで致命性を下げる。
- SBT コントラクトは譲渡不可設計で不正二次流通を抑止。

## 今後の拡張アイデア

- バックエンド導入と集計 API 化
- 店舗別ダッシュボード（発行 SBT / 来店頻度分析）
- 動的 SBT（来店回数でメタデータ自動更新）
- ソーシャル共有（獲得 SBT カードを画像化）
- ENS / Lens 等アイデンティティ統合
- プライバシー配慮位置情報（グリッド丸め / 離散化）

## 将来の夢

現在はブラウザのローカルストレージに保存していますが、事業化できたらデータベースを活用して、堅牢で安定してサービス提供をしたい。

## セットアップ（抜粋）

```bash
# 依存インストール
pnpm install

# 開発サーバ起動
pnpm dev

# 環境変数例 (.env.local)
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXX
NEXT_PUBLIC_DEFAULT_CHAIN_ID=80002
```

## 参考ドキュメント

- `QR_FORMAT_SPECIFICATION.md`: QR 仕様詳細
- `WALLET_CONNECTOR_CODE.md`: ウォレット接続ロジック
- `IMPLEMENTATION_GUIDE.md`: 実装ガイド

## 誤字修正メモ

- 「ECR-20」→ 正しくは「ERC-20」
- 「こを使っているところ」→ 「これを使っているところ」

必要に応じて本文へ反映済み。追加で修正したい表現があればコメントください。

## おわりに

“支払う”行為にコレクション体験を重ね合わせることで、継続来店インセンティブを自然に設計できる可能性を感じました。ステーブルコイン決済 UX の改善と、SBT 活用事例の一助になれば幸いです。ご意見・改善案お待ちしています。

---
（公開前に: 公開設定 / タイトル微調整 / 画像差し込み 等をご確認ください）
